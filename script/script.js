
var main = {
	init: function () {
		/*var wrapper, windowHeight, windowWidth, navHeight;
		wrapper = $('#wrapper');
		windowHeight = parseInt($(window).height(), 10);
		windowWidth = parseInt($(window).width(), 10);
		navHeight = 34;
		wrapper.height('1000px');	//windowHeight - navHeight + 'px'
		wrapper.css('background-size', windowWidth + 'px, ' + windowHeight);*/
	}


};


$(document).ready(main.init);